<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE math4u2 PUBLIC "-//FH Augsburg//DTD Math4u2//DE" "http://www.math4u2.de/xml/dtd/math4u2DTD.dtd">
<math4u2>
	<head>
		<version>2.0</version>
		<author>Stefan Fenn, Erich Seifert, Prof. Dr. Maximilian Weiß</author>
		<module id="main" class="ShowMeGraph"/>
	</head>
	<body>
		<location>2. Bedienung</location>
		<title>2.2 Objekte definieren und löschen</title>
		<step>
			<title>2.2 Objekte definieren und löschen:<p/>Definitionsfeld und Definitionsliste</title>
			<description>			
<p/><img src="math4u2/doc/images/bedienung/definitionsfeld01.gif"/>

<p/><strong>Objekt definieren</strong>
<br/>Objekte werden durch Eingabe der entsprechenden Definition im 
Definitionsfeld (z.B. <code>f(x):=x*sin(x)</code>) erzeugt. Eine Definition 
wird durch Eingabe von RETURN wirksam. 
<br/>Jedes Objekt wird durch einen Eintrag in der Definitionsliste repräsentiert.
<br/>Neue Einträge werden oben in der Liste angefügt.

<p/><strong>Definition ändern</strong>
<br/>Sie ändern die Definition eines Objekts mit dem Namen <code>obj</code>, 
indem Sie für den gleichen Namen im Definitionsfeld eine neue Definition eingeben.
<p/>Wenn Sie die Definition von <code>obj</code> nur leicht modifizieren wollen, 
dann klicken Sie auf den entsprechenden Eintrag der Definitionsliste. 
Die aktuelle Definition wird dadurch in das Definitionsfeld gestellt. Sie
können die Definition jetzt dort editieren und zuletzt mit RETURN bestätigen.
<p/>Für ein bereits existierendes Objekt kann eine neue Definition nur dann 
eingegeben, wenn dadurch ein Objekt des gleichen Typs definiert wird. 
So kann z.B. eine einstellige Funktion <code>f(x):=sin(x)</code>
nur durch eine einstellige Funktion wie <code>f(x):=2*cos(x)</code> ersetzt werden
(und z.B. nicht durch eine zweistellige Funktion, einen Punkt oder eine Strecke).

<p/>Wenn die Definition der Funktion <code>f(x):=sin(x)</code> nicht mehr 
benötigt wird und nun ein Punkt mit dem Namen <code>f</code> definiert werden 
soll, dann muss zunächst die Definition der Funktion gelöscht werden
(verschieben in den math4u2-Papierkorb), anschließend kann mit 
<code>f:=punkt(1,1)</code> der Punkt definiert werden.

<p/><strong>Objekt löschen</strong>
<br/>Zum Löschen eines Objekts haben Sie zwei Möglichkeiten:
<br/>
<ul>
	<li>Markieren Sie das entsprechende Element der Definitionsliste mit der Maus und drücken Sie die
Taste ENTF.</li>
	<li>Schieben Sie das entsprechende Element der Definitionsliste mit gedrückter Maustaste in den
math4u2-Papierkorb.</li>
</ul>
<br/>Mit einem Objekt werden auch sämtliche zugehörigen Graphen und gegebenenfalls 
die zugehörige Detailsicht gelöscht.
<p/><strong>Achtung:</strong>
 Ein Objekt kann nicht gelöscht werden, wenn es von anderen Objekten noch verwendet
wird.
			</description>
		</step>
	</body>
</math4u2>
