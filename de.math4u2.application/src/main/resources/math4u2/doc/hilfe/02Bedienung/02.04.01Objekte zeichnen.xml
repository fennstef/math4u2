<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE math4u2 PUBLIC "-//FH Augsburg//DTD Math4u2//DE" "http://www.math4u2.de/xml/dtd/math4u2DTD.dtd">
<math4u2>
	<head>
		<version>2.0</version>
		<author>Stefan Fenn, Erich Seifert, Prof. Dr. Maximilian Weiß</author>
		<module id="main" class="ShowMeGraph"/>
	</head>
	<body>
		<location>2. Bedienung</location>
		<title>2.4.1 Objekte Zeichnen: drag and drop</title>
		<step>
			<title>2.4.1 Objekte Zeichnen: drag &amp; drop</title>
			<description>			
Ein Objekt wird gezeichnet, indem man den entsprechenden Eintrag der 
Definitionsliste mit gedrückter linker Maustaste auf die gewünschte 
Zeichenfläche zieht und dort die Taste losläßt (drag &amp; drop).
<p/>
Wenn zu einem Objekt eine Detailsicht existiert, dann kann man auch diese 
Detailsicht auf die Zeichenfläche ziehen.
<p/>
Je nach Typ des Objekts wird es jetzt auf der Zeichenfläche durch einen 
spezifischen Graphen dargestellt. Wichtige Beispiele sind:
<br/>
<ul>
	<li>
		Zu einer einsteligen Funktion wie <code>f(x):=x*sin(x)</code> 
		wird der Funktionsgraph gezeichnet.
	</li>
	<li>
		Ein Punkt <code>p:=punkt(1,1)</code> zeichet sich, er kann mit 
		der Maus verschoben werden, seine Koordinaten ändern sich dann 
		entsprechend.	
	</li>
	<li>
		Die anderen affinen Objekte(<code>kreis</code>,<code>flaeche</code>, <code>kurve</code>,
		<code>bezier</code>, <code>kurvenzug</code> und <code>wikel</code>) zeichnen sich
		wie gewohnt.
	</li>
	<li>
		Eine Sonderstellung hat der zweidimensionale Vektor: Will man 
		einen zweidimensionalen Vektor wie <code>vec:=vektor({1,2})</code>
		als Pfeil darstellen, so ist dieser Pfeil streng genommen nicht das
		Bild des Vektors, sondern das Bild eines Repräsentanten. Dies bildet 
		man beim Zeichnen wir folgt nach:
		<br/>Zu <code>vec</code> definiert man z.B. mit 
		<code> pv1:=pfeil(punkt(1,1),vec)</code> einen Repräsentanten, konkret
		mit dem Aufpunkt <code>punkt(1,1)</code>. Dieser Repräsentant 
		<code>pv1</code> zeichnet sich nun auf der Zeichenfläche als Pfeil. 
		Die Pfeilspitze kann mit der Maus bewegt werden, damit ändern sich die
		Koordinaten des Vekors <code>vec</code>. Wenn man mit 
		<code>pv2:=pfeil(punkt(3,-1),vec)</code> einen weiteren Repräsentanten 
		zu <code>vec</code> definiert und dann zeichnet, zeichnen sich 
		<code>pv1</code> und <code>pv2</code> stets als parallelgleiche Pfeile.	
	</li>
</ul>
				
<p/><img src="math4u2/doc/images/bedienung/objekte-zeichnen01.gif"/>
<p/><strong>Graph von Zeichenfläche löschen:</strong> Kontextmenü der Zeichenfläche
öffnen (rechte Maustaste), über das Element "Lösche Graph" die gewünschte Auswahl treffen.
			</description>
		</step>
	</body>
</math4u2>